OUT_PATH="" # absolute path for outputs (with "/" ending)
BASE_PATH="" # absolute location of this file (with "/" ending)
DB_DRIVER="" # name of the DB driver (name of the file in share/lib/db/)
DB_DSN="" # db connection config 
.Share$graph.lib='std' # graph lib to use "std" is standard R graph device
.Share$platform='fr' # name of the "platform" => name of the file in share/platform